# Utilizário para gerar eventos em broker de mensageria para testes

### Instalação no MACOS
```sh
brew install librdkafka
```
>Obs: Para utilizar integração com apache kafka no MACOS é necessário instalar a lib acima
